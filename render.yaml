services:
  - type: web
    name: remote-desktop-relay
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node relay_server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
